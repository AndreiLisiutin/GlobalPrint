@using Microsoft.AspNet.Identity
@using GlobalPrint.ClientWeb.Helpers
@using System.Globalization

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul id="main-nav-right" class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Мои Принтеры", "MyPrinters", "Printer")
            </li>            
            <li>
                @Html.ActionLink("Мои заказы", "MyOrders", "Order")
            </li>
            <li>
                @{Html.RenderAction("UserRecievedPrintOrder", "UserPrintInfo");}       
            </li>
            <li>
                @Html.ActionLink(User.Identity.GetUserName(), "UserProfile", "UserProfile", new { }, new { @class = "font-bold" })
            </li>
            <li>
                @{Html.RenderAction("UserBalance", "UserPrintInfo");}
            </li>
            <li>
                <a href="javascript:document.getElementById('logoutForm').submit()" class="font-bold">Выйти</a>
            </li>
            <li>
                @Html.ActionLink("О проекте", "About", "About")
            </li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Зарегистрироваться", "Register", "Account", null, new { @class = "font-bold", id = "registerLink" })
        </li>
        <li>
            @Html.ActionLink("Войти", "Login", "Account", null, new { @class = "font-bold", id = "loginLink" })
        </li>
        <li>
            @Html.ActionLink("О проекте", "About", "About")
        </li>
        @{Html.RenderAction("CountryPicker", "UserPrintInfo");}
    </ul>
}


