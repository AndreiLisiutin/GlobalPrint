@using GlobalPrint.ClientWeb.Models.FeedbackViewModel
@model FeedbackViewModel

@{
    ViewBag.Title = "Обратная связь";
    Layout = "~/Views/Shared/_NavigationLayout.cshtml";
}
@section Scripts{
    <script src="~/Resources/Scripts/GlobalPrint/Feedback/FeedBack.js"></script>
}

<div class="container verticalWrap">
    <div class="loginForm">
        <div class="loginTitle">
            Обратная связь
        </div>

        @using (Html.BeginForm("Feedback", "Feedback", FormMethod.Post, new { @id = "feedbackForm" }))
        {
            <div class="loginFormLabel input-wrapper">
                <p>E-Mail</p>
                @Html.TextBoxFor(m => m.Email, new { @class = "loginFormInput", @type = "email", @autofocus = true })
            </div>

            <div class="loginFormLabel input-wrapper">
                <p>Имя пользователя</p>
                @Html.TextBoxFor(m => m.UserName, new { @class = "loginFormInput" })
            </div>

            <div class="loginFormLabel input-wrapper">
                <p>Тема</p>
                @Html.TextBoxFor(m => m.Subject, new { @class = "loginFormInput" })
            </div>

            <div class="loginFormLabel input-wrapper">
                <p>Сообщение</p>
                @Html.TextAreaFor(m => m.Message, 10, 55, new { @class = "loginFormInput feedback-message" })
            </div>

            <div class="row" style="margin-top:15px;">
                <div class="col-md-7 col-xs-12"></div>
                <div class="col-md-5 col-xs-12" align="right">
                    <button class="goReg" type="submit">Отправить</button>
                </div>
            </div>
            
        }
    </div>
</div>
