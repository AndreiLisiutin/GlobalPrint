@using GlobalPrint.ClientWeb
@model ResetPasswordViewModel

@{
    ViewBag.Title = "Изменение пароля";
    Layout = "~/Views/Shared/_NavigationLayout.cshtml";
}

<div class="container verticalWrap">
    <div class="loginForm">
        <div class="loginTitle">Изменение пароля</div>

        @using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post, new { role = "form" }))
        {
            @Html.AntiForgeryToken()

            @Html.HiddenFor(m => m.Code)

            <div class="loginFormLabel">
                <p>E-Mail</p>
                @Html.TextBoxFor(m => m.Email, new { @class = "loginFormInput", @type = "email", @required = "true", @readonly = "true" })
            </div>
            <div class="loginFormLabel">
                <p>Новый пароль</p>
                @Html.TextBoxFor(m => m.Password, new { @class = "loginFormInput", @type = "password", @required = "true" })
            </div>
            <div class="loginFormLabel">
                <p>Подтвердите новый пароль</p>
                @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "loginFormInput", @type = "password", @required = "true" })
            </div>

            <div class="row" style="margin-top:15px;">
                <div class="col-md-7 col-xs-12"></div>
                <div class="col-md-5 col-xs-12" align="right">
                    <button class="goReg" type="submit">Измененить</button>
                </div>
            </div>
        }

    </div>
</div>
